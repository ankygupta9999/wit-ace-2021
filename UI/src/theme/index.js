import {colors} from './colors';

const font = 'Arial, Helvetica, sans-serif';

const fonts = {
	heading: font,
	body: font,
	mono: font
};

export const theme = {
	colors,
	fonts,
	styles: {
		global: {}
	}
};
